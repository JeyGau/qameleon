{
	"version": "2.0.0",
	"tasks": [
        {
            "label": "Build and Install src",
            "type": "shell",
            "command": "mkdir -p build && cd build && cmake .. && make && sudo make install",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Builds and installs the src/ repository"
        }
	]
}